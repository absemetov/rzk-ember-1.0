
import { listItem } from 'rzk-ember/helpers/list-item';
import { module, test } from 'qunit';

module('Unit | Helper | list item');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = listItem([42]);
  assert.ok(result);
});

