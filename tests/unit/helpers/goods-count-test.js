
import { goodsCount } from 'rzk-ember/helpers/goods-count';
import { module, test } from 'qunit';

module('Unit | Helper | goods count');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = goodsCount([42]);
  assert.ok(result);
});

