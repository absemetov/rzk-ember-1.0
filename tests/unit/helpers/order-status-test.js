
import { orderStatus } from 'rzk-ember/helpers/order-status';
import { module, test } from 'qunit';

module('Unit | Helper | order status');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = orderStatus([42]);
  assert.ok(result);
});

