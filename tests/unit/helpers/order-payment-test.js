
import { orderPayment } from 'rzk-ember/helpers/order-payment';
import { module, test } from 'qunit';

module('Unit | Helper | order payment');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = orderPayment([42]);
  assert.ok(result);
});

